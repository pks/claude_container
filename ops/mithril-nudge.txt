MITHRIL SPOT INTERRUPTION SIGNALED ({{STATUS}}). Node terminates at {{END_TIME}}. Stop launching new long-running jobs. Commit pending changes to git. Write /workspace/STATUS.md summarizing in-flight work and what to resume next. touch /workspace/.shutdown-acked and exit. /workspace and {{HOME_PATH}} are bind-mounted from host-persistent storage so they survive; only in-flight processes will be killed. The next run picks up via --continue with a resume prompt.
